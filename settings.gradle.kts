rootProject.name = "MPDebugger"


pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenLocal()
        mavenCentral()
        // limework maven server no longer exists.
        maven("https://repo.papermc.io/repository/maven-public/")
    }
}
